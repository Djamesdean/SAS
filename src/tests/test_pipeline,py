import pytest


# Test text preprocessing
def test_preprocess_text():
    sample_text = "This game is AMAZING!!!"
    processed_text = preprocess_text(sample_text)
    assert isinstance(processed_text, str)
    assert "amazing" in processed_text.lower()
    assert "!!!" not in processed_text  # Should remove punctuation

# Test model training
def test_train_models():
    assert train_naive_bayes() is not None
    assert train_logistic_regression() is not None
    assert train_lstm() is not None